# Database Server

This system represents a protected server/database role.

Document the service role, network segment, exposed ports, monitoring agent, data classification used in tests, and firewall dependencies. Use synthetic data only and never publish real database contents or credentials.
