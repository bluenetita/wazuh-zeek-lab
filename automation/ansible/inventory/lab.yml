---
all:
  children:

    # Tutte le VM che hanno un Wazuh Agent
    wazuh_agents:
      children:
        zeek_sensors:
        #linux_clients:

    # Sensori Zeek
    zeek_sensors:
      hosts:
        zeek01:
          ansible_host: 10.3.10.4
          ansible_user: prova
          ansible_ssh_private_key_file: ~/.ssh/id_ecdsa

    ## In futuro aggiungerai qui i client
    #linux_clients:
    #  hosts: {}