# RouterOS Interfaces

Document physical, virtual, bridge, and VLAN interfaces used by the laboratory.

For each interface include name, type, connected segment, address, VLAN role, and purpose. Preserve the relationship between RouterOS interfaces and Proxmox bridges.

Use `/interface print detail` and `/ip address print detail` for validation, but do not publish hardware serial numbers or unrelated production interfaces.
