# pfSense Firewall Rules

Document only the rules required to reproduce controlled communication between AttackerVM and the internal laboratory.

For each rule include interface, direction, protocol, source, destination, port, action, and purpose. Keep broad allow rules to a minimum and explain temporary testing exceptions.

RouterOS quarantine rules are documented separately under [`../routeros/`](../routeros/).
