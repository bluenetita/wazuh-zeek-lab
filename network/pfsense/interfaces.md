# pfSense Interfaces

Document the pfSense interfaces that connect the simulated external network, RouterOS/internal transit, and any management network.

For each interface record:

- logical name;
- Proxmox bridge;
- address and prefix;
- connected security zone;
- DHCP/static status;
- purpose.

Do not publish public addresses, VPN secrets, certificates, or credentials.
