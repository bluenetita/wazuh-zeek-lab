# RouterOS VLANs

This document maps RouterOS VLAN interfaces to the laboratory roles defined in [`../vlans.md`](../vlans.md).

For each VLAN record ID, interface/bridge, subnet, gateway address, DHCP behavior, and connected VM roles.

Keep VLAN identifiers aligned with Proxmox tagging and pfSense policy.
