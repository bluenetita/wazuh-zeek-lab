# pfSense Logging

Document firewall and system logs used during validation.

Include the logging location, rule labels, time synchronization, and how events are correlated with Zeek and Wazuh timestamps. Publish only reduced, sanitized examples.
