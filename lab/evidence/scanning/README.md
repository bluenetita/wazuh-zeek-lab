# Scanning Evidence

`scanning-sanitized-samples.log` contains one reduced example for each current scanning event type. The sample schema uses `src_ip` consistently because the current Wazuh correlation rules match that field.

The original `scanning.log` and rotated `.gz` archives remain outside the public repository.
