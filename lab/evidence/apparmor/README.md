# AppArmor Evidence

`apparmor-shell-denied-sanitized.log` is a reduced AppArmor/Audit record showing a denied shell-execution attempt by the controlled inventory service.

It is sufficient to validate decoder/rule behavior without publishing the complete Auditd archive.
