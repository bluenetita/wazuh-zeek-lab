# Wazuh Evidence — Reverse Shell

Store reduced Wazuh alerts that demonstrate:

- Zeek base/custom rule matches;
- Auditd endpoint connection rule `110900`;
- correlation rules `120900–120926`;
- Active Response execution;
- successful or failed RouterOS quarantine.

For each sample record the rule ID, level, agent, relevant fields, and interpretation. Sanitize IPs, usernames, paths, UIDs, filenames, and timestamps where required.

Do not publish complete alert archives, webhooks, SSH material, or evidence tarballs.
